{
  "name": "app",
  "version": "1.0.0",
  "description": "note",
  "main": "index.js",
  "scripts": {
    "gulp:default": "gulp default",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "moon",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-uglify": "^3.0.0"
  }
}
